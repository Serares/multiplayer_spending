postgres:
  connection:
    type: postgres
    database: spending_local
    secret: ''
    secretReadOnly: /secrets/api/database-readonly/
    resourceArn: ''
    region: ''
    host: localhost
    port: 49153
    username: postgres
    password: password
  logging: false
